<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppLocalize_se.Views.Usuarios.LoginView">

    <ContentPage.Content>
        <VerticalStackLayout>
            <Entry Margin="0,10,0,0" Placeholder="Insira o seu Email" Text ="{Binding Login}"
                   VerticalOptions="FillAndExpand"></Entry>
            <Entry Margin="0,10,0,10" Placeholder="Insira a sua Senha" Text="{Binding Senha}" IsPassword="True"
                   VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand"></Entry>
            <Button Text="Entrar" Command="{Binding AutenticarCommand}" Margin="0,10,0,0"/>
            <HorizontalStackLayout
                 HorizontalOptions="Center" Spacing="20" Margin="30">
                <Label Text="Não possui login?"></Label>
                <Label Text="Cadastre-se" FontAttributes="Bold"></Label>
                <HorizontalStackLayout.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding DirecionarCadastroCommand}">
                    </TapGestureRecognizer>
                </HorizontalStackLayout.GestureRecognizers>
            </HorizontalStackLayout>
        </VerticalStackLayout>


    </ContentPage.Content>
</ContentPage>