<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppLocalize_se.Views.Usuarios.CadastrarView"
             Title="Cadastrar">
    <VerticalStackLayout>

        <Entry Placeholder="Digite seu nome Completo" 
               Text ="{Binding Nome}" 
               Margin="0,10,0,0" 
               VerticalOptions="FillAndExpand" 
               HorizontalOptions="FillAndExpand">
        </Entry>

        <Entry Placeholder="Digite seu Rm" 
               Text ="{Binding Rm}"
               Margin="0,10,0,0" 
               VerticalOptions="FillAndExpand" 
               HorizontalOptions="FillAndExpand">
        </Entry>

        
        <Entry Placeholder="Digite a senha" 
               Text="{Binding Senha}" 
               IsPassword="True"
               Margin="0,10,0,0" 
               VerticalOptions="FillAndExpand"
               HorizontalOptions="FillAndExpand" >
        </Entry>


            <Button Text="Registrar" Command="{Binding RegistrarCommand}" Margin="0,10,0,0"/>
    </VerticalStackLayout>
</ContentPage>